# flutter_geofence_plugin_example

App di esempio del plugin: registra aree di geofencing dalla UI e mostra
una notifica locale (via `flutter_local_notifications`) a ogni ingresso o
uscita, anche ad app terminata. Istruzioni di test nel README del plugin.
